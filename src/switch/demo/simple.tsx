import React, { FC } from 'react';
import { Switch } from '@sensoro/sensoro-design';

const BasicExample: FC = () => {
  return (
    <div>
      <Switch checked />
    </div>
  );
};

export default BasicExample;
