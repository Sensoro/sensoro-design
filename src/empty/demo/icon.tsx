import React, { FC } from 'react';
import { Empty } from '@sensoro/sensoro-design';

const Example: FC = () => {
  return <Empty icon="image" iconStyle={{ fontSize: 40 }} description="" />;
};

export default Example;
