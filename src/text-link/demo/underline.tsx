import React, { FC } from 'react';
import { TextLink } from '@sensoro/sensoro-design';

const UnderlineExample: FC = () => {
  return (
    <div>
      <TextLink underline>基本链接</TextLink>
    </div>
  );
};

export default UnderlineExample;
