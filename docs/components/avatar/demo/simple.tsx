import React, { FC } from 'react';
import { Avatar } from '@sensoro/sensoro-design';

const Example: FC = () => {
  return (
    <div>
      <Avatar />
      <Avatar shape={'square'} />
    </div>
  );
};

export default Example;
