import React, { FC } from 'react';
import { TextLink } from '@sensoro/sensoro-design';

const DisableExample: FC = () => {
  return (
    <div>
      <TextLink disabled>确认</TextLink>
    </div>
  );
};

export default DisableExample;
